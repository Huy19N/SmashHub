# API Contracts

We are utilizing an existing Backend API endpoint. No new contracts are created.

### Cancel Booking
- **Method**: `DELETE`
- **Path**: `/api/bookings/{bookingId}`
- **Auth**: Required (Bearer Token)
- **Response**: `200 OK` (ApiResponse)
